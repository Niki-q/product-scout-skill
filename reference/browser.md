# Browser session discipline

Placeholder — not written yet. Will cover:

- No parallel tabs/sessions against the same marketplace — a shared browser
  driven by concurrent agents produces mismatched title/price/link data.
- CAPTCHA encountered → stop and surface it, never try to solve or route
  around it.
- Compact indexed-text page representation (numbered clickable/readable
  elements) instead of dumping a full accessibility snapshot into context
  for every search.
