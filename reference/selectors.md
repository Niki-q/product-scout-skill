# Marketplace selectors

Placeholder — not written yet. Will be a table of DOM selectors / URL query
patterns per marketplace (Amazon, eBay, AliExpress), each row tagged with a
verification status:

- `V` — verified against a live page.
- `U` — candidate, unverified — capture and confirm before trusting.
